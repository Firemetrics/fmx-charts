{{- if .Values.fuego.enabled -}}
{{- $name := printf "%s-fuego" .Release.Name -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/part-of: {{ include "appName" . }}
      app.kubernetes.io/component: backend
      app.kubernetes.io/name: {{ include "fuego.podLabel" . }}
  template:
    metadata:
      name: {{ $name }}
      labels:
        app.kubernetes.io/part-of: {{ include "appName" . }}
        app.kubernetes.io/component: backend
        app.kubernetes.io/name: {{ include "fuego.podLabel" . }}
    spec:
      serviceAccountName: {{ include "resourceReaderSaName" . }}
      {{- if .Values.registryAuth.enabled }}
      imagePullSecrets:
        - name: {{ include "registry.secretName" . }}
      {{ end }}
      initContainers:
        - name: wait-for-db-bootstrap
          image: bitnami/kubectl:latest
          command:
            - /bin/bash
            - -c
            - |
              echo "Waiting for database bootstrap job to complete..."
              until kubectl get job/{{ include "db.bootstrapJob" . }} -o jsonpath='{.status.conditions[?(@.type=="Complete")].status}' | grep True; do
                echo "Not complete yet..."
                sleep 5
              done
              echo "Bootstrap job is complete."
      containers:
        - name: main
          image: {{ .Values.fuego.image | quote }}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: FUEGO_SERVER_INTERFACE
              value: "0.0.0.0"
            - name: FUEGO_SERVER_PORT
              value: "3000"
            {{- if .Values.ingress.enabled }}
            - name: FUEGO_SERVER_PUBLIC_URL
              value: https://{{ .Values.ingress.host }}
            {{- end }}
            - name: FUEGO_DATABASE_HOST
              value: {{ include "db.svcName" . | quote }}
            - name: FUEGO_DATABASE_PORT
              value: "5432"
            - name: FUEGO_DATABASE_DBNAME
              value: {{ .Values.db.pg.dbname | quote }}
            - name: FUEGO_DATABASE_USERNAME
              value: {{ .Values.db.pg.user | quote }}
            - name: FUEGO_DATABASE_PASSWORD
              value: {{ .Values.db.pg.password | quote }}
            {{- if .Values.fuego.oidc.enabled }}
            {{- $discoveryUrl := .Values.fuego.oidc.discoveryUrl }}
            {{- if not $discoveryUrl }}
              {{- $discoveryUrl = include "keycloak.openidDiscoveryUrl" . }}
            {{- end }}
            {{- if $discoveryUrl }}
            - name: FUEGO_OIDC_ENABLE
              value: "true"
            - name: FUEGO_OIDC_DISCOVERY_URL
              value: {{ $discoveryUrl | quote }}
            {{- end }}
            {{- end }}
{{- end -}}
