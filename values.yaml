registryAuth:
  enabled: false
  server: "repo.firemetricshealth.com"
  username: ""
  password: ""

ingress:
  enabled: false
  host: ""

db:
  image: repo.firemetricshealth.com/ghcr/firemetrics/spilo16:3.3-p2-latest
  pg:
    dbname: firemetrics
    user: postgres
    password: postgres
  requestedStorage: 10Gi

fuego:
  enabled: true
  image: repo.firemetricshealth.com/ghcr/firemetrics/fuego:0ba35303
  oidc:
    enabled: true
    discoveryUrl: ""
    audience: ""

panel:
  enabled: true
  image: repo.firemetricshealth.com/ghcr/firemetrics/fmx-panel:cc066e1
  oidc:
    discoveryUrl: ""
    clientId: "panel"
    audience: ""

keycloak:
  enabled: true

  importRealm:
    enabled: true
    configMapName: keycloak-import
    realmName: firemetrics

  persistence:
    enabled: true
    claimName: keycloak-data
    size: 1Gi

  production: true
  httpRelativePath: /auth/
  proxyHeaders: xforwarded
  extraStartupArgs: "--import-realm"
  externalDatabase:
    host: "none"
  extraVolumes:
    - name: keycloak-data
      persistentVolumeClaim:
        claimName: keycloak-data
    - name: keycloak-import
      configMap:
        name: keycloak-import
  extraVolumeMounts:
    - name: keycloak-data
      mountPath: /opt/bitnami/keycloak/data/h2
    - name: keycloak-import
      mountPath: /opt/bitnami/keycloak/data/import
  postgresql:
    enabled: false
  extraEnvVars:
    - name: KEYCLOAK_DATABASE_VENDOR
      value: "dev-file"
  externalDatabase:
    password: notused
