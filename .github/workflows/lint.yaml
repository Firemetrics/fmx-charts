name: Lint

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  image-tags:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check parent and leaf image tags are consistent
        run: ./scripts/bump-images.sh --check
