# fmx-monitoring

![Version: 2.0.0](https://img.shields.io/badge/Version-2.0.0-informational?style=flat-square)

Cluster monitoring with the Alloy collector, Prometheus, and kube-state-metrics

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| alloy.image | string | `"grafana/alloy:v1.12.2"` | The image used for the Alloy collector pods. |
| imagePullSecret | string | `""` | The image pull secret used for pulling images. |
| kubeStateMetrics.image | string | `"registry.k8s.io/kube-state-metrics/kube-state-metrics:v2.16.0"` | The image used for the kube-state-metrics pod. |
| kubeStateMetrics.resources | list | `["pods"]` | The Kubernetes resource types kube-state-metrics exports metrics for (the ClusterRole rules are derived from this list). Only pod metrics are consumed by the shipped dashboard; extend together with metrics.keepList. |
| logs.enabled | bool | `false` | Enable the log pipeline (ship pod logs to Loki). |
| logs.jsonProcessing.labelFields | object | `{"fmx_audit_event_type":"fields.\"fmx.audit_event_type\"","fmx_service_name":"fields.\"fmx.service_name\"","level":"level"}` | Map of indexed Loki label name to JSON expression (JMESPath, as accepted by Alloy's `stage.json`). Use plain field names for top-level keys (`level: level`) or nested expressions for nested keys (`fmx_audit_event_type: 'fields."fmx.audit_event_type"'`). The `fmx_*` defaults are required by fmx-panel's audit logs page. |
| logs.lokiUrl | string | `"http://loki.default.svc.cluster.local"` | Base URL of the Loki instance to push logs to. |
| logs.namespaces | list | `[]` | List of namespaces to collect logs from. Empty list means all namespaces. |
| metrics.enabled | bool | `true` | Enable the metrics pipeline (Prometheus, kube-state-metrics, and all metric scrapes performed by Alloy). |
| metrics.keepList | list | `["up","probe_success","node_cpu_seconds_total","node_memory_MemTotal_bytes","node_memory_MemAvailable_bytes","node_filesystem_size_bytes","node_filesystem_avail_bytes","container_cpu_usage_seconds_total","container_memory_working_set_bytes","kubelet_volume_stats_capacity_bytes","kubelet_volume_stats_used_bytes","kubelet_volume_stats_available_bytes","kube_pod_info","kube_pod_status_phase","kube_pod_status_ready","kube_pod_container_status_restarts_total","traefik_entrypoint_requests_total","traefik_entrypoint_request_duration_seconds_sum","traefik_entrypoint_request_duration_seconds_count","certmanager_certificate_expiration_timestamp_seconds"]` | Metric names stored in Prometheus; everything else is dropped before remote write. Every entry is consumed by the shipped FMX Cluster Health dashboard. Extend this list before adding dashboard panels that need additional metrics — history for a metric starts when its name is added (KSM metrics may also require extending kubeStateMetrics.resources). |
| metrics.scrapeInterval | string | `"60s"` | The interval applied to all metric scrapes and probes. |
| probes.targets | list | `[]` | HTTP health endpoints probed from inside the cluster. Each entry needs a `name` and a `url`; results are stored as `probe_*` metrics. Populated by fmx-instance with the health endpoints of the enabled components. |
| prometheus.image | string | `"docker.io/prom/prometheus:v3.5.0"` | The image used for the Prometheus pod. |
| prometheus.persistence.enabled | bool | `true` | Enable persistence for Prometheus data. |
| prometheus.persistence.requestedStorage | string | `"5Gi"` | The size of the Prometheus data volume. |
| prometheus.retentionSize | string | `"4GB"` | Size-based retention cap for stored metrics (hard backstop; keep below the data volume size). |
| prometheus.retentionTime | string | `"10d"` | Time-based retention for stored metrics. |
| scrape.certManager.address | string | `"cert-manager.cert-manager.svc.cluster.local:9402"` | Address of the cert-manager Prometheus metrics endpoint. |
| scrape.certManager.enabled | bool | `true` | Scrape cert-manager. Disable if the cluster uses its own certificate management instead of the cert-manager installed by fmx-cluster. |
| scrape.traefik.address | string | `"traefik-metrics.traefik.svc.cluster.local:9100"` | Address of the Traefik Prometheus metrics endpoint. |
| scrape.traefik.enabled | bool | `true` | Scrape the Traefik ingress controller. Disable if the cluster uses its own ingress instead of the Traefik installed by fmx-cluster. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
