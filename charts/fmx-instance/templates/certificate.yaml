{{- if and .Values.tls.enabled (not .Values.tls.certSecret.existingSecret) .Values.tls.certManager.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "appName" . }}
spec:
  commonName: {{ .Values.hostname | quote }}
  secretName: {{ include "tlsCertSecretName" . }}
  dnsNames:
    - {{ .Values.hostname | quote }}
  issuerRef:
    kind: ClusterIssuer
    name: {{ .Values.tls.certManager.issuer | quote }}
{{- end }}
